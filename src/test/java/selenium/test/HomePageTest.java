package selenium.test;

import org.testng.Assert;
import org.testng.annotations.Test;
import selenium.page.HomePage;
import selenium.page.ResultPage;
import selenium.utils.TestConstant;

public class HomePageTest extends BaseHomePageTest {
/*    private HomePage homePage;
    private ResultPage resultPage;



    public void testSearchContent () {
        homePage = new HomePage(driver);
        resultPage = new ResultPage(driver);
        homePage.inputSearchAndNext(TestConstant.SEARCH_CONTENT);
        System.out.println(TestConstant.EXPECTED_STRING);
        System.out.println(resultPage.getResultName());
        Assert.assertTrue(resultPage.getResultName().contains(TestConstant.EXPECTED_STRING));
    }


    public void test() {
        System.out.println("Test");
        homePage = new HomePage(driver);
        resultPage = new ResultPage(driver);
    }*/

}
